[build]
  # functions = "functions"
  # This will be run the site build
  command = "NODE_ENV=production npm run export"
  # This is the directory is publishing to netlify's CDN
  publish = "__sapper__/export"

[dev]
  # Local dev command. A.k.a npm start
  command = "sapper dev"
